//#include "Projectile.h"
//#include <thread>

/*
void Projectile::SendIt()
{
	while (isActive)
	{
		m_y--;
		std::this_thread::sleep_for(std::chrono::milliseconds(1000));
		if (m_y == 0)
		{
			isActive = false;
		}
	}
}

void Projectile::SetType(char a)
{
	switch (a)
	{
	case 'b':
		type = 'o';
	case 'm':
		type = '^';
	default:
		break;
	}
}
*/
